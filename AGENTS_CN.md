# StreamView Agent 规则

这些规则适用于本仓库内的 AI 辅助开发。

## 范围控制

- 不要在一次改动里尝试实现完整的 StreamEye 替代品。
- 不要一次性实现完整的 H.264/H.265 解析。语法支持必须按小范围、可测试的切片逐步增加。
- 不要引入新的第三方依赖，除非先在 `docs/ADR/` 中说明原因。
- parser 代码必须保持独立，不依赖 GUI，也不依赖 FFmpeg adapter。

## Parser 正确性

- 每个 parser 功能都必须有测试或 golden output。
- 不能用“FFmpeg 可以解码”来证明我们的 parser 是正确的。
- 解析代码必须做边界检查，不能越界读取。
- 对畸形输入不能无限循环。
- 新增 codec 语法字段时，需要在代码注释或 `docs/CODEC-SCOPE.md` 中说明标准来源。

## 改动卫生

- 只修改当前任务必须修改的文件。
- 不要格式化无关代码。
- 不要删除已有代码或注释，除非任务明确要求。
- 优先使用职责清晰的小模块。

## 验证要求

- 声称完成前必须运行相关测试。
- parser 改动应尽量包含畸形输入覆盖。
- CLI 输出变更必须更新 golden tests。
